# ScraperBot
Little side project to scrape the website
Instructions

1. First run the following commands in terminal
```
pip install -r requirements.txt
python -m playwright install chromium
```

2. Then fill up the .env file according to the fields

3. Run the next few commands
```
python main.py
```